# 测试与覆盖率

房间预约系统后端测试基建，2026-09-15 首次建立，配套 docs/进度.md 与 docs/代码仓实现清单.md。

## 一、测试栈与命令
1. 依赖：spring-boot-starter-test，范围 scope=test，含 JUnit 5、Mockito 与 AssertJ，不引入额外测试框架。
2. 运行：`mvn -B test`，首次构建需下载依赖，之后单轮约 40 秒。
3. 覆盖率：JaCoCo 0.8.15，绑定 test 阶段，报告输出 target/site/jacoco/index.html，机器可读数据在 target/site/jacoco/jacoco.csv。
4. 本机运行前提：JAVA_HOME 指向 IDEA 自带 JBR 17，即 `E:\env\IntelliJ IDEA 2024.1.7\jbr`；Maven 用 `E:\env\apache-maven-3.8.1`，本地仓库为默认的 C 盘 .m2。

## 二、用例清单，2026-09-15
| 测试类 | 用例数 | 覆盖重点 |
| --- | --- | --- |
| BookingServiceImplTest | 21 | 预约十步校验各分支、对内琴房会员权限、周预约与周退约上限、时段冲突、唯一键兜底转 409、退约写状态与空出提醒 |
| RateLimitServiceTest | 6 | 阈值内放行、超限 429、开关关闭放行、Redis 异常回退进程内计数、未登录按 IP 限流 |
| GlobalExceptionHandlerTest | 5 | 业务异常、风控异常携带 data、字段校验返回字段名、兜底 500 |
| TokenUtilsTest | 4 | 载荷为 用户id-角色、密码 hash 验签、改密后旧 token 失效、两小时过期 |
| EventBusServiceTest | 4 | 本地消息表按 normal 与 degraded 落库、投递成功置 sent、失败标记 broker 不可达并累计重试 |
| EventConsumeServiceTest | 4 | 首次处理写幂等记录、重复投递丢弃、主键冲突按已处理返回、非法载荷忽略 |
| VacancyNotifyServiceTest | 3 | 匹配关注者写消息并置已提醒、无关注者不写、载荷不完整直接返回 |
| JwtInterceptorTest | 8 | 公开接口放行、非控制器放行、缺 token 与非法载荷 401、用户不存在 401、改密后验签失败 401、角色不匹配 403、合法放行 |
| AuthControllerTest | 5 | 字段非法返回 400 与字段名、注册成功、登录签发 token 且不回传密码、找回返回 sent 与本地令牌、邮箱不存在不暴露注册状态 |
| BookingControllerTest | 3 | 创建绑定当前用户、冲突转 409、退约调用服务 |
| AdminEventsControllerTest | 3 | 异步模式返回计数、降级模式 null 字段不报错、按每分钟 30 次限流 |
| CacheServiceTest | 8 | 开关穿透、TTL 写入、命中反序列化、坏值剔除、Redis 降级进程内、击穿保护单飞、回源空值不写、前缀 SCAN 失效 |
| RiskServiceTest | 7 | 窗口内计次达阈值拉黑、受限拦截带解除时间、窗口外不计次、开关关闭、无用户默认态、Redis 计分与过期、Redis 降级 |
| ActivityServiceTest | 8 | 开关与管理员过滤、未知事件零权重、周月双 ZSET 写入、退约负分、降级排序与脱敏、同分稳定排序与分页、无昵称匿名 |
| 合计 | 89 | 全部通过，零失败零跳过 |

前端并行的用例在 frontend/user-web/src/utils 下，vitest 运行 `npm.cmd run test`：time.test.js 2 项、request.test.js 4 项、rate.test.js 6 项，共 12 项。

## 三、覆盖率基线
1. 整体：行 30.8%，即 665/2157 行，全仓 71 个类。
2. 分层：公共层 42.9%，实体与 DTO 37.5%，业务层 33.3%，接口层 10.3%。
3. 已覆盖重点类：JwtInterceptor、AdminEventsController、EventConsumeService、GlobalExceptionHandler 与两类异常均为 100%，BookingServiceImpl 95.2%，CacheService 90.8%，RateLimitService 92.1%，ActivityService 82.2%，VacancyNotifyService 80.6%，RiskService 76.9%，EventBusService 68.3%，AuthController 61.5%。
4. 未覆盖集中区：MqAvailabilityService 由端到端自测覆盖；其余 controller 只覆盖了鉴权、预约与事件概览三个，管理端 service 与 mapper 层未纳入单测，由验收脚本与端到端覆盖。
4. 整体比例偏低是因为 71 个类里大部分未纳入首轮，属预期基线，不构成回归信号；核心预约规则与限流链路已在高覆盖区间。

## 四、写法约定
1. 纯单元测试不启动 Spring 上下文，依赖用 @Mock 加 @InjectMocks 注入，保证单轮秒级完成。
2. 规则参数统一走 `getInt(key, default)` 的默认值返回，测试与 rule_config 种子解耦。
3. 涉及 LambdaUpdateWrapper.set 的用例，需在 @BeforeAll 中用 TableInfoHelper.initTableInfo 注册实体，否则纯单测环境拿不到实体元数据，报 can not find lambda cache for this entity。
4. 日期类用例统一取 LocalDate.now().plusDays(1) 构造，避开「当天只能预约当前时刻之后」与自然周滚动带来的时间敏感。
5. MyBatis-Plus 的 BaseMapper 存在 insert(T) 与 insert(Collection) 双重重载，mock 参数要写具体类型，如 any(Booking.class)，写无参 any() 会编译报引用不明确。
6. 新增业务分支时同步补用例，保持 BookingServiceImpl 这类核心类覆盖率不下降。

## 五、后续可选
1. 需要时给 JaCoCo 加 check 规则设行覆盖门槛；当前基线偏低，暂不设卡，避免构建被覆盖率卡住。
2. controller 层可改用 MockMvc 做接口级测试，与现有纯单元测试并行。
3. 并发与端到端验证仍走 tools/bench_concurrency.py 与 tools/demo_test.ps1，单测不重复承担。

更新日期：2026-09-17。变更：补 MockMvc 与拦截器测试 19 项、阶段 A 与 B 服务测试 23 项，用例合计 89 项，覆盖率升至行 30.8%。
