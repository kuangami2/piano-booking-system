# 房间预约系统：进度记录

## 一、里程碑
1. 2026-09-06 需求定稿 v1，见 需求说明.md；仓库 room-reservation 初始化；讨论结论归档，双端 Web 留小程序空间、3+3 脚手架骨架、Vue3 加 Element Plus、并发规则快照、乐器独立表、信用人工减分。
2. 2026-09-07 任务书与建表脚本入库；docs 定稿文档同步；schema 补并发唯一键；接口清单与页面清单初稿。
3. 2026-09-07 后端实现：骨架验收通过后落地用户端核心模块，commit 3279b9d；冒烟测试通过琴房过滤、空闲区间、时长校验；接口清单修订 v0.2 对齐实现。

## 二、当前阶段
实现阶段进行中：用户端核心模块已编码，成功预约与冲突、退约空出提醒待完整自测；管理端模块与双端 Web 待开发。

## 三、阶段计划
1. 任务书提交，开题材料准备。
2. 实现：3+3 脚手架改造 backend，建库导表，后端模块按分工推进。
3. 双端 Web：frontend 用户端与管理端，共用接口。
4. 测试：冲突拦截、退约提醒、减分、规则改参等场景自测。
5. 课设报告与答辩材料。

## 四、问题与解决
1. schema 初版缺并发兜底唯一键，booking 补 uk_book_slot，watch 补 uk_watch_slot。
2. MyBatis-Plus 更新误用 LambdaQueryWrapper，set 找不到符号，改 LambdaUpdateWrapper 修复。
3. 本机无独立 JDK 17，IDEA 以自带 JBR 编译；外部文件修改后需 Ctrl+Alt+Y 同步再编译。

更新日期：2026-09-07
