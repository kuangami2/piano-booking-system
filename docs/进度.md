# 房间预约系统：进度记录

## 一、当前阶段
1. 需求已定稿 v1，见 需求说明.md。
2. 题目定为基于 Spring Boot 与 Vue 的琴房预约管理系统设计与实现。
3. 组号 3，成员林义洋 2023211715 组长、刘善宁 2023211716、孙业腾 2023211723，分工见 协作规范.md 与 任务分配.md。
4. 协作底座：Gitee 私有仓 room-reservation，仓库本地 E:\Pycharm_Python\room-reservation。
5. v1 功能已完成并集成验收，master 保持稳定版；v1.1 阶段 A 与阶段 B 均已实现并验收并入 develop。
6. 阶段 B 已完成前后端联调收口，接口清单升 v0.6，四处分歧项修正后冻结；阶段 C 的 MQ 事件总线与提醒异步化待验收后评估。
7. 分支状态：master 为发布主线，develop 为集成基线，feature/v1.1-enhance 为增强开发分支，组员个人分支合入 develop 后同步。
8. 演示环境：后端 9090、用户端 5173、管理端 5174；本机未安装 Redis，阶段 A、B 的缓存、限流、计分与风控按设计回退进程内实现。
9. 阶段 C 方案选定真装 RabbitMQ：本机 E:\env 已装 Erlang OTP 27.2 与 RabbitMQ 4.3.6，管理插件已启用，服务为按需手动启动，AMQP 5672 与管理台 15672 本机可用；评审草案已成稿于 接口评审_阶段C.md，刘善宁已全部同意 12 项，待孙业腾回复，通过后升接口清单 v0.7 冻结再实现。

## 二、阶段计划
1. 任务书提交，开题材料准备。
2. 设计：表结构定稿 schema.sql，接口与页面清单。
3. 实现：backend 骨架与业务模块，frontend 用户端与管理端。
4. 测试：冲突拦截、退约提醒、减分、规则改参等场景自测。
5. 课设报告与答辩材料。

## 三、已完成节点
1. 2026-09-07 主线搭建完成：docs 定稿同步，schema 补并发唯一键，接口与页面清单成稿；后端骨架落地并本机验收通过；用户端核心模块实现琴房浏览与空闲查询、预约规则校验与行锁冲突检测、退约空出提醒、关注、消息、反馈与内容接口；定时任务与规则缓存、双端前端骨架、验收脚本与演示清单就绪。commit 3279b9d、ca1b978、c095f80、0bf23fb、3a937f9。
2. 2026-09-08 v1 集成收口：两分支合并零冲突，验收脚本 19 项 PASS，master 推送 672efe7；演示修复 d54e854；组员反馈处理 843b5cb 补 mine 接口 roomName 并同步 develop；接口清单升 v0.3。
3. 2026-09-08 阶段 A 实现 commit fe8de5a：缓存与失效、限流、索引与连接池、性能文档与压测脚本；2026-09-09 压测回填，读接口 P50 降 26% 至 29%，功能验证缓存命中 139.9 降至 11.1ms、管理端改名即时生效、第 11 次预约返回 429；合入 develop 4e3e221。
4. 2026-09-09 需求第四节精简 0e7bfdb 与时长下限按粒度覆盖 18e8351；用户端接入 roomName、操作状态与限流提示 a276813，排行榜预留页完善 47ac33e，均已合入 develop。
5. 2026-09-10 阶段 B 接口评审全员通过，接口清单升 v0.5 冻结，排行榜与临近取消风控按冻结契约实现；后端验收通过 commit 43dfeef，含唯一约束生成列修正，前端与管理端接入待完成。

## 四、问题与解决
1. schema 初版缺并发兜底唯一键，booking 补 uk_book_slot，watch 补 uk_watch_slot。
2. IDEA 编译报源发行版 17，Project SDK 指向 IDEA 自带 JBR JDK 17 解决。
3. 外部改文件后 IDEA 需 Ctrl+Alt+Y 同步再编译。
4. PowerShell 调 curl 引号转义与中文乱码，改用 Invoke-RestMethod，脚本内容一律 ASCII。
5. 改密接口 500：IGNORED 策略下空实体 updateById 全字段覆盖，改 LambdaUpdateWrapper 仅更新密码列。
6. 注入 StringRedisTemplate 须写 @Resource(name = "stringRedisTemplate")，否则按字段名命中 RedisTemplate。
7. 阶段 A 编译坑：新增类文件须确认已复制入库，否则调用方报找不到符号。

6. 2026-09-12 阶段 B 前后端联调收口：脚本实测排行榜周月榜结构、风控三次临近取消进黑名单与第四次预约 409、规则批量更新与缓存刷新；浏览器实测排行榜页真实数据、受限态提示与按钮禁用、管理端 18 条参数保存即时生效；修正预约创建返回体、受限态次数语义、排行榜统计范围与日期序列化四处问题，接口清单升 v0.6；演示数据复位，演示残留参数 weeklyCancelLimit 改回 2。

7. 2026-09-12 晚 功能迭代四条全部落地：需求一注册唯一性补学号与邮箱唯一索引；需求二引入参数校验，注册与改密字段级校验，登录保持宽松；需求三新增 password_reset 令牌表与找回重置接口，令牌 30 分钟一次性并作废旧令牌；需求四前缀失效改 SCAN、未登录按 IP 限流、缓存击穿单飞、并发压测脚本。实测：找回全链路与令牌一次性通过，50 并发 P50 11.8ms、100 并发 P50 9.2ms 零失败，10 并发抢同一时段仅 1 条成功。同轮发现并修复 MyBatis 标准输出 SQL 日志造成的串行化瓶颈，吞吐由 27 QPS 提升至 1867 QPS。迁移脚本 migration_v1_2.sql 与 migration_v1_3.sql。

8. 2026-09-15 合入组员用户端节流 commit b20864d：刘善宁新增 frontend/user-web/src/utils/rate.js，带尾部补偿的 throttle 并在卸载时 cancel，Rooms 与 RoomDetail 两页接入；合并零冲突，前端构建通过。

9. 2026-09-15 后端测试基建建立：pom 引入 spring-boot-starter-test 与 JaCoCo 0.8.15，新增 src/test 下四个测试类共 36 项用例，覆盖预约十步校验各分支、对内琴房权限、周次数与周退约上限、唯一键兜底转 409、限流与 Redis 降级、异常统一返回与 token 改密失效；mvn test 全绿，整体行覆盖 11.6%，BookingServiceImpl 95.9%；同轮补 user-web 的 rate.test.js 六项，前端用例合计 12 项。本轮另在 E:\env 落地阶段 C 运行环境 Erlang OTP 27.2 与 RabbitMQ 4.3.6，管理插件启用，服务按需手动启动，实测常驻内存 118MB。


10. 2026-09-15 阶段 C 评审草案成稿 docs/接口评审_阶段C.md，commit ec74306：定事件总线范围与投递模型、消息信封、本地消息表与幂等、消费语义与死信、降级开关、配置项与演示口径共十二项要点；建议方案为只异步化退约提醒、topic 交换机加死信、本地消息表加 processed_event 幂等、broker 不可达时降级同步执行提醒。

11. 2026-09-15 合入组员用户端找回重置 commit c3f1839：Login.vue 补找回与重置表单校验、加载态、成功提示与令牌回填，邮箱不存在时沿用不暴露注册状态的提示；ActivityRanking.vue 更新为 v0.6 冻结口径；组长复核前端 12 项单测通过、生产构建 1694 模块成功，合并零冲突，merge 提交 c1659b6；同轮刘善宁回复阶段 C 评审 12 项全部同意。


12. 2026-09-15 阶段 C 评审通过并冻结接口清单 v0.8：刘善宁与孙业腾均回复十二项同意，孙业腾另提四条补充并承接管理端事件概览页；接口清单新增第十八节冻结事件总线与概览接口契约，页面清单升 v0.3；阶段 C 进入实现。


13. 2026-09-15 阶段 C 后端实现与自测通过：pom 引入 spring-boot-starter-amqp，新增 event_outbox 与 processed_event 两表及 migration_stage_c.sql；EventBusService 与业务同事务落表并在提交后投递，MqAvailabilityService 负责可达性探测与降级；VacancyNotifyService 抽出提醒逻辑供异步与降级共用，EventConsumeService 以 eventId 幂等，VacancyNotifyListener 手动确认且异常进 DLQ，OutboxRetryTask 重投 pending 与死信且计数独立；管理端新增 GET /api/admin/events/summary。单测 47 项全绿，整体行覆盖 14.9%；端到端实测异步路径 12 项与降级路径 13 项全部通过。


14. 2026-09-15 验收脚本修复与端到端补测：demo_test.ps1 的姓名、学号与密码改为符合需求二校验的格式，此前字段校验上线后脚本已跑不通；补阶段 C 断言七项，含管理端概览可达、运行模式、outbox 计数、死信深度与事件计数，脚本合计 26 项断言全通过。同轮端到端验证死信链路，发现并修复两处实现缺陷：全局 MessageConverter 被监听容器抢先用于转换，重投带 x-death 的消息被直接丢弃，以及死信重投以 basicGet 取出后未显式确认，消息滞留并反复入队；修复后死信按 x-death 计数重投、达上限确认丢弃，redelivered 计数正确。

15. 2026-09-17 合入孙业腾管理端交付 commit 1513292，集成提交 071502f：新增事件概览页 Events.vue 与组件测试，接入 /api/admin/events/summary，降级时给出提示且 outbox 与 dlq 为 null 时显示占位；规则页 18 条参数按预约、信用、活跃度、风控分组并用单元格合并展示；tools/demo_test.ps1 补场景 5 减分暂停与场景 6 规则即时生效断言，并把跳过提示移除。组长复核：管理端 vitest 3 项通过、生产构建 1687 模块成功，端到端脚本 34 项全过；复核期间 RabbitMQ 服务处于停止状态，脚本因此在降级模式下运行，场景 4 提醒仍送达、场景 C 返回 degraded 与 null 字段不报错，等价于补验了降级路径。

16. 2026-09-17 测试增强与控制器层补齐：新增 JwtInterceptorTest 8 项、AuthControllerTest 5 项、BookingControllerTest 3 项、AdminEventsControllerTest 3 项、CacheServiceTest 8 项、RiskServiceTest 7 项、ActivityServiceTest 8 项，用例总数由 66 升至 89 项全绿；整体行覆盖由 14.9% 升至 30.8%，其中 JwtInterceptor 与 AdminEventsController 100%、CacheService 90.8%、ActivityService 82.2%、RiskService 76.9%。同轮发现并修复 ActivityService 本地降级分页的 off-by-one 缺陷，即 0 起闭区间上界与 1 起排名混用导致 Redis 不可用时每页少返回一条，本机未装 Redis 恰好走该路径。

17. 2026-09-17 演示前检测登记：实测复现一项缺陷与四项需求。缺陷为管理端取消预约只通知关注者、预约人无感知；三项属设计取舍或交互预期，即同账号自约自关注再自退时提醒按设计排除本人、管理端有 token 时初始界面被重定向到首页、用户端无未读公告弹窗；一项为缺失功能，即管理端无临时关闭琴房时段并释放预约的能力。结论已登记至需求说明第十四节与记忆故障排查，待评审后实现。

18. 2026-09-17 修复取消与退约的通知缺口：管理端取消预约原先只通知关注者，现同时给预约人发 booking_cancelled 消息；用户退约后本人也收到确认消息，空出提醒仍按设计只发其他关注者。同轮把管理端重复的提醒实现并入 VacancyNotifyService，异步消费者与同步降级两条路径口径一致；新增测试 7 项，用例合计 96 项全绿；端到端复验管理端取消、本人退约与跨账号空出提醒三条链路均通过。

19. 2026-09-17 预约交互提示与排序修复，含三项核查：用户端预约提交原先在所选区间不属于空闲时段与风控受限时静默返回，导致提示偶尔失效，现改为逐分支明确提示，仅重复点击保持静默；双端预约记录排序由时段倒序改为按自增主键倒序，即最新预约靠前。核查结论三项均为正常或设计使然：临期退约惩罚实测三次退约达阈值后第四次预约返回 409 并给解除时间，黑名单时长按规则默认 60 分钟；管理端用户列表主动将 password 置空属安全设计，密码为 BCrypt 哈希且提供管理员重置入口；关注提醒后记录状态转为 notified，同一条关注不会重复提醒，重新关注会新建记录。

20. 2026-09-17 双端登录失效与消息体验修复，并起草阶段 D 草案：管理端请求拦截原按 HTTP 状态码判断 401，而本项目业务码在响应体、HTTP 恒为 200，导致 token 失效后不跳登录页，现改为按业务码处理，与用户端口径一致；用户端消息红点原先只在路由切换时刷新，标记已读后计数不动，现抽出共享未读状态并在标记已读与全部已读后即时刷新，同时新增 PUT /api/messages/read-all 批量已读接口。另起草 docs/接口评审_阶段D.md，覆盖可约区间变更的预约同步通知与用户周配额管理两项需求，含数据模型、接口草案与分期建议，待评审。

21. 2026-09-17 登记轮播图裁剪上传需求：现场确认用户端轮播容器高度写死、宽度随屏幕变化导致比例不固定，管理端上传无尺寸指引；确定整合为头像式裁剪上传，比例锁定 21:9、前端按 1260×540 导出后上传，用户端容器同步锁定比例兜底；不改接口故无需评审，裁剪组件可复用。

更新日期：2026-09-17。变更：登记轮播图裁剪上传需求，此前补消息接口用例与双端登录失效修复。
