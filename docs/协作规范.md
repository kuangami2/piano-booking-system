# 房间预约系统：协作开发规范

## 一、版本控制决策，2026-09-06
1. 托管平台选 Gitee，私有仓库 room-reservation。
2. 三人协作，代码与文档同仓，docs 目录放开发文档。
3. 仓库结构：backend 后端、frontend 前端、docs 文档。
4. 分支模型：Git Flow，实际主干 master，feature 并入 develop 集成，验证后合 master。
5. 提交规范：模块加内容，如 booking 实现冲突检测；动手与提交前先 pull。
6. 仓内放 AGENTS.md，写明结构、构建命令、提交规范，各 agent 进仓先读。

## 二、模块分工减冲突
1. 按模块切分：琴房、预约、信用、规则、反馈、前端各自范围。
2. 接口契约先行，docs 内先定接口清单，见 接口清单.md。

## 三、本地记忆独立机制
1. 共享层：git 仓存全量文档，三人共同更新。
2. 个人层：E:\Agent\agent_memory 只归本人，不进 git，风格按个人记忆规范。
3. 同步流程：git 有更新时，本地 agent 拉取变更，按记忆规范摘录关键状态入个人记忆，不整体并入。
4. .gitignore 排除本地记忆路径。

## 四、本机 Git 准备
1. WSL 已装 git 2.34.1，SSH 公钥已生成并添加 Gitee，验证通过。
2. 全局身份已配：user.name lyy，user.email 1138848718@qq.com。
3. 仓库本地路径 E:\Pycharm_Python\room-reservation，保持 Windows 侧便于 PyCharm。

## 五、成员与分工
1. 林义洋 2023211715 组长，后端预约核心，冲突检测、空出提醒、信用与规则逻辑。
2. 刘善宁 2023211716 用户端页面（`frontend/user-web`）；后端接口缺口列清单交组长补，不直接修改 `backend`。
3. 孙业腾 2023211723 管理端页面及对应后端接口与统计。

v1.1 候选增强仍遵循上述目录边界：RabbitMQ、Redis、事件幂等与风控服务由后端负责人实现，刘善宁只负责用户端排行榜和风控提示的 REST 接入。

更新日期：2026-09-07
